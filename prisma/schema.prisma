generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model SecureUrl {
  id    Int     @id @default(autoincrement())
  path String  @unique
  name  String
  mimetype String
}
